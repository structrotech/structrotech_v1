import { StatusScreen } from "@/components/StatusScreen";

export default function AuthPage() {
  return (
    <StatusScreen
      title="Authentication coming soon"
      description="Account sign-in and sign-up are not available yet. You can still browse articles, categories, and resources without an account."
    />
  );
}
